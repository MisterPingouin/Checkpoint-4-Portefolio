{% extends 'base.html.twig' %}

{% block title %}Edit Project{% endblock %}

{% block body %}
    <div class="container mx-auto px-4">
        <h1 class="text-xl font-bold mb-4">Edit Project</h1>
        {{ include('project/_form.html.twig', {'button_label': 'Update'}) }}
        <a href="{{ path('app_project_index') }}" class="inline-block bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded mt-4">Back to list</a>
        {{ include('project/_delete_form.html.twig') }}
    </div>
{% endblock %}
