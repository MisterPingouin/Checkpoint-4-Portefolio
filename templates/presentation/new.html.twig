{% extends 'base.html.twig' %}

{% block title %}New Presentation{% endblock %}

{% block body %}
<div class="container mx-auto px-4">
    <h1 class="text-xl font-bold mb-4">Create new Presentation</h1>

    {{ include('presentation/_form.html.twig', {'button_label': 'Create'}) }}

    <a href="{{ path('app_admin_presentation', {'projectId': projectId}) }}" class="inline-block bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded mt-4">Back to list</a>
</div>
{% endblock %}
