{% extends 'base.html.twig' %}

{% block title %}Presentation{% endblock %}

{% block body %}
    <h1>Presentation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ presentation.id }}</td>
            </tr>
            <tr>
                <th>Fonction</th>
                <td>{{ presentation.fonction }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ presentation.description }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ presentation.link }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_presentation', {'projectId': presentation.project.id}) }}">back to list</a>

    <a href="{{ path('app_presentation_edit', {'id': presentation.id}) }}">edit</a>

    {{ include('presentation/_delete_form.html.twig') }}
{% endblock %}
